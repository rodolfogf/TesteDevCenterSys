import React from "react";
import { Paper } from "@mui/material";

const List = ({ children }) => {
    

    return (
        <Paper>
            {children}
        </Paper>
    );
};

export default List;
